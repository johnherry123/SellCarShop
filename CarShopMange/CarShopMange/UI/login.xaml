<Window x:Class="CarShopMange.UI.login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="800" Width="1000"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True"
        WindowStyle="None"
        Background="Transparent"
        ResizeMode="NoResize"
        MouseDown="Window_MouseDown"
        >

    <Grid >

       
        <Grid.Background>
            <ImageBrush ImageSource="pack://application:,,,/Resource/hinh-nen-bau-troi-binh-minh-hoang-hon-3.jpg.webp" />
        </Grid.Background>

  
        <Grid VerticalAlignment="Top" Height="35">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

           

            <Button Grid.Column="1" Content="—" Width="35" Height="35"
                    Click="Minimize_Click"
                    Background="Transparent" BorderBrush="{x:Null}" Foreground="White"/>

            <Button Grid.Column="2" Content="⬜" Width="35" Height="35"
                    Click="Maximize_Click"
                    Background="Transparent" BorderBrush="{x:Null}" Foreground="White"/>

            <Button Grid.Column="3" Content="X" Width="35" Height="35"
                    Click="Close_Click"
                    Background="Transparent" BorderBrush="{x:Null}" Foreground="White"/>
        </Grid>

      
        <Border Width="400" Height="380"
                VerticalAlignment="Center"
                CornerRadius="10"
                Padding="30">

           
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#50FFFFFF" Offset="0"/>
                    <GradientStop Color="#20FFFFFF" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>

          
            <Border.Effect>
                <DropShadowEffect 
                    BlurRadius="15"
                    ShadowDepth="2"
                    Direction="270"
                    Color="#66000000"
                    Opacity="0.5"/>
            </Border.Effect>

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,20,0,0">

                <TextBlock Text="LOGIN"
           FontSize="30"
           FontWeight="SemiBold"
           FontFamily="Segoe UI Semibold"
           Foreground="White"
           HorizontalAlignment="Center"
           Margin="0,0,0,30">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="#99000000" BlurRadius="4" ShadowDepth="1" Opacity="0.6"/>
                    </TextBlock.Effect>
                </TextBlock>
                <Grid Margin="0,0,0,15">
                    <TextBox x:Name="UsernameTextBox"
                             Height="40"
                             Padding="10"
                             FontSize="14"
                             Background="#30FFFFFF"
                             Foreground="White"
                             BorderBrush="Transparent"
                             BorderThickness="0"
                             TextChanged="TextBox_TextChanged"
                             GotFocus="TextBox_GotFocus"
                             LostFocus="TextBox_LostFocus"/>
                    <TextBlock x:Name="UsernamePlaceholder"
                               Text="Username"
                               Foreground="#AAFFFFFF"
                               FontSize="14"
                               IsHitTestVisible="False"
                               Margin="15,0,0,0"
                               VerticalAlignment="Center"
                               Visibility="Visible"/>
                </Grid>

               
                <Grid Margin="0,0,0,15">
                    <PasswordBox x:Name="PasswordBox"
                                 Height="40"
                                 Padding="10"
                                 FontSize="14"
                                 Background="#30FFFFFF"
                                 Foreground="White"
                                 BorderBrush="Transparent"
                                 BorderThickness="0"
                                 PasswordChanged="PasswordBox_PasswordChanged"
                                 GotFocus="PasswordBox_GotFocus"
                                 LostFocus="PasswordBox_LostFocus"/>
                    <TextBlock x:Name="PasswordPlaceholder"
                               Text="Password"
                               Foreground="#AAFFFFFF"
                               FontSize="14"
                               IsHitTestVisible="False"
                               Margin="15,0,0,0"
                               VerticalAlignment="Center"
                               Visibility="Visible"/>
                </Grid>

               
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <CheckBox Content="Remember me" Foreground="White" FontSize="12"/>
                    <TextBlock Text="Forgot password?" Foreground="White" FontSize="12"
                               TextDecorations="Underline" HorizontalAlignment="Right"
                               Cursor="Hand" Grid.Column="1"/>
                </Grid>

              
                <Button Content="Login" Width="300" Height="45" FontWeight="SemiBold"
                        Background="White" Foreground="#6f00ff"
                        BorderBrush="#6f00ff" BorderThickness="1"
                        Cursor="Hand" Margin="0,0,0,20">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border CornerRadius="25"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                
                <TextBlock HorizontalAlignment="Center" FontSize="12" Foreground="White">
                    <Run Text="Don't have an account? "/>
                    <Run Text="Register" Foreground="Wheat" TextDecorations="Underline" Cursor="Hand"/>
                </TextBlock>

            </StackPanel>
        </Border>
    </Grid>
</Window>
